CREATE TABLE userEntity (
    id INTEGER NOT NULL PRIMARY KEY,
    latitude REAL NOT NULL,
    longitude REAL NOT NULL
);

getUserLocation:
SELECT *
FROM userEntity;

insertUser:
INSERT OR REPLACE
INTO userEntity
VALUES (?,?,?);
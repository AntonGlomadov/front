CREATE TABLE userEntity (
    number TEXT NOT NULL PRIMARY KEY,
    password TEXT NOT NULL
);

getUserData:
SELECT *
FROM userEntity;

insertUser:
INSERT OR REPLACE
INTO userEntity
VALUES (?,?);